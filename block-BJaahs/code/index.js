const testData = {
  title: 'Where is the capital of Jordan',
  options: ['Tashkent', 'Amaan', 'Kuwait City', 'Nairobi'],
  correctAnswerIndex: 1,
  function:function isAnswerCorrect(){
    testData.options=0;
    if(testData.options===this.options[i]){
      testData.options=
    }
  }
};